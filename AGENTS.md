# 人生ルーレット: 作業ルール

## プロジェクト概要

- HTML / CSS / JavaScript による、同一画面またはオンラインで2〜4人が遊ぶ人生すごろく。オンライン部屋のみ Supabase JavaScript CDN を利用する。
- 公開先は GitHub Pages: <https://umepyon.github.io/life-roulette/>
- Nintendo や他社ゲームのキャラクター、ロゴ、素材を使わない。画像・演出はオリジナルで作る。

## ユーザーとのやり取り

- ユーザーを「うめぴょん」と呼ぶ。
- 変更前に目的・対象・確認方法を短く共有する。
- 調査だけの依頼では、明示的な実装依頼がない限りファイルを変更しない。
- 不明点が実装方針を大きく左右する場合だけ質問する。それ以外は、影響の小さい妥当な前提を明示して進める。

## Git・公開

- コードを変更する前に、対象ファイルを含む現状をコミットする。未コミットのユーザー変更は勝手に混ぜない。
- 変更後は、対象ファイルを限定してステージし、意図の分かるコミットを作る。
- ユーザーが公開を求めた、または既存の公開フローを継続する作業では、`main` に push して GitHub Pages のビルド完了を確認する。
- 破壊的な Git 操作（`reset --hard`、広範囲の削除など）は行わない。

## 現在のゲーム仕様

- 所持金はマイナスを許可する。
- 結婚・子ども誕生では、ほかの参加者がサイコロでご祝儀を決める。金額は 1万 / 2万 / 3万 / 5万 / 7万 / 10万円で、4万円は使わない。
- 結婚・子ども誕生のご祝儀は、まだゴールしていない参加者だけが払う。ゴール済みの参加者からは徴収しない。
- ほかの全員がゴールした時点で、残った1人はビリとして自動的にゴールし、結果を表示する。
- 誰でもゴール時に運命サイコロを振る。出目1は「カジノで大損」で−1,000万円、出目2〜6は所持金の変化なし。
- マイナス所持金でゴールした場合は、性別別の節約暮らしエンディングを表示する。
- 生成コースとフォールバックコースには、「3マスもどる」「1回休み」「一番前の人と場所を入れ替える」を必ず1つずつ含める。
- GOALの1つ手前は必ず「スタートに戻る」。止まると所持金を半分にしてSTARTへ戻す。
- PC盤面では、プレイヤーのオープンカーに結婚・子どもに応じた人数を乗せる。
- イベントは場面に合ったオリジナルのアニメ調画像を表示する。
- オンライン部屋ではホスト端末がゲーム状態を管理し、参加者は部屋コードで入室して自分に割り当てられた番だけ操作する。ホストが閉じると部屋は終了する。
- `supabase-config.js` には公開URLと publishable key のみを置く。secret key / service_role key / パスワードは絶対にコミットしない。

## レイアウト方針

- PCは複数ルートが分かるボードを表示する。
- PCのヘックス盤面は大きなアイコンのみを表示し、マス名・説明文は入れない。着地時は専用イベント画像または出来事カードで内容と金額を表示する。
- ヘックス盤面ではイベント／チャンスマスを青、分岐マスを赤で表示する。
- ヘックス盤面は接続線用のマスを使わず、実際に進める隣接ヘックス同士を直接つなぐ。ルートとして接続しないヘックスは接触させず、通常マスは前後2方向、分岐・合流だけ最大3方向の接続にする。
- スマホ幅（現在は 760px 以下、および幅900px以下・高さ500px以下の横向きスマホ）では、後方視点のオープンカーと奥へ続く四角いマス目を表示する。出目の数だけ1マスずつ進む。iPad幅ではPC盤面を使う。
- スマホではタイトル説明を隠し、盤面を最初に見せる。「みんなの人生」は初期状態で閉じたネイティブの `details` 要素にし、各参加者にはゴールまでの残りマス数を表示する。
- スマホの順番表示はモバイル盤面内に1か所だけ表示する。サイコロ欄では名前と所持金を人物画像の横にまとめ、順番を重複表示しない。
- スマホ右上はハンバーガーメニューにし、「新しいゲーム」と「ボード全体を表示」を出す。全体盤面は横向きの端末でも全体が収まるようにする。
- 幅390px × 高さ844pxでは、盤面・折りたたみ欄・サイコロボタンまで初期画面に収める。変更時はこの基準を崩さない。
- `viewport-fit=cover` と `safe-area-inset` を維持し、iPhone Safari、iPad Safari、Android Chrome の実機または代表ビューポートで確認する。詳細は `課題.md` を参照する。

## コース生成の方針

- 通常の新規ゲームでは、`hex-course.js` がシード付き・制約付きの道を生成し、`app.js` がそれを移動・分岐・イベントに接続する。
- PCはヘックス盤面と接続線を描画し、スマホは選択済みの経路を四角いマス目の後方視点で描画する。
- `LEGACY_COURSE` と静的な盤面定義は、生成に失敗したときだけのフォールバックとして扱う。
- シードは画面表示済み。コピー・入力・同一シードの再プレイは `課題.md` の未完了項目である。
- コース規模は小（メイン27マス）・中（メイン60マス）・大（メイン100マス）。START、GOAL、必須イベント、分岐数、ルート長、合流、収支バランスを検証してから盤面を表示する。
- 道の生成とイベント内容のランダム配置は分離する。管理者が `data/events.csv` を編集し、イベント候補は200種類まで登録できる。ゲーム内では出現頻度を考慮して重複を抑え、収支の偏りを確認する。

## 検証

- JavaScript変更後: `node --check app.js` と `git diff --check` を実行する。
- CSV変更後: `node tests/event-catalog.test.js` を実行し、列・ID・金額・イベント種別・画像名を検証する。
- ヘックス生成変更後: `node tests/hex-course.test.js` を実行し、複数シードの生成と妥当性を確認する。
- 画面変更後: PCとスマホ幅390pxで、初期画面・人物選択・サイコロ・分岐・イベントを確認する。
- アニメーションを無効にするユーザー設定でも、操作可否と進行状況が分かることを確認する。
- 公開時: GitHub Pages のビルド状態と、公開URLから変更ファイルが取得できることを確認する。

## 参照資料

- 次回課題と実装順: `課題.md`
- 遊び方と起動方法: `README.md`
