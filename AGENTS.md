# 人生ルーレット: 作業ルール

## プロジェクト概要

- 依存関係なしの HTML / CSS / JavaScript による、同一画面で2〜4人が遊ぶ人生すごろく。
- 公開先は GitHub Pages: <https://umepyon.github.io/life-roulette/>
- Nintendo や他社ゲームのキャラクター、ロゴ、素材を使わない。画像・演出はオリジナルで作る。

## ユーザーとのやり取り

- ユーザーを「うめぴょん」と呼ぶ。
- 変更前に目的・対象・確認方法を短く共有する。
- 調査だけの依頼では、明示的な実装依頼がない限りファイルを変更しない。
- 不明点が実装方針を大きく左右する場合だけ質問する。それ以外は、影響の小さい妥当な前提を明示して進める。

## Git・公開

- コードを変更する前に、対象ファイルを含む現状をコミットする。未コミットのユーザー変更は勝手に混ぜない。
- 変更後は、対象ファイルを限定してステージし、意図の分かるコミットを作る。
- ユーザーが公開を求めた、または既存の公開フローを継続する作業では、`main` に push して GitHub Pages のビルド完了を確認する。
- 破壊的な Git 操作（`reset --hard`、広範囲の削除など）は行わない。

## 現在のゲーム仕様

- 所持金はマイナスを許可する。
- 結婚・子ども誕生では、ほかの参加者がサイコロでご祝儀を決める。金額は 1万 / 2万 / 3万 / 5万 / 7万 / 10万円で、4万円は使わない。
- 最初にゴールした人は、サイコロで100万〜1,000万円のボーナスを得る。
- マイナス所持金でゴールした場合は、性別別の節約暮らしエンディングを表示する。
- PC盤面では、プレイヤーのオープンカーに結婚・子どもに応じた人数を乗せる。
- イベントは場面に合ったオリジナルのアニメ調画像を表示する。

## レイアウト方針

- PCは複数ルートが分かるボードを表示する。
- スマホ幅（現在は 760px 以下）では、後方視点のオープンカーと奥へ続く四角いマス目を表示する。出目の数だけ1マスずつ進む。
- スマホではタイトル説明を隠し、盤面を最初に見せる。「いまの人生」は初期状態で閉じたネイティブの `details` 要素にする。
- 幅390px × 高さ844pxでは、盤面・折りたたみ欄・サイコロボタンまで初期画面に収める。変更時はこの基準を崩さない。
- iPhone Safari、iPad Safari、Android Chrome の実機または代表ビューポートで確認する。詳細は `課題.md` を参照する。

## コース生成の今後の方針

- 現在は `app.js` の `SPACES`、`ROUTE_LINES`、`BRANCH_SIGNPOSTS` が固定盤面を表している。
- 今後はヘックス座標の盤面で、シード付き・制約付きの道をゲーム開始時に生成する。
- START、GOAL、必須イベント、分岐数、ルート長、合流、収支バランスを検証してから盤面を表示する。
- 道の生成とイベント内容のランダム配置は分離する。イベント候補は約100種類まで増やし、同一ゲーム内の重複と収支の偏りを抑える。

## 検証

- JavaScript変更後: `node --check app.js` と `git diff --check` を実行する。
- 画面変更後: PCとスマホ幅390pxで、初期画面・人物選択・サイコロ・分岐・イベントを確認する。
- アニメーションを無効にするユーザー設定でも、操作可否と進行状況が分かることを確認する。
- 公開時: GitHub Pages のビルド状態と、公開URLから変更ファイルが取得できることを確認する。

## 参照資料

- 次回課題と実装順: `課題.md`
- 遊び方と起動方法: `README.md`
