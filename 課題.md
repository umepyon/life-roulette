# 次回の課題

最終更新: 2026-07-31

## 今日の到達点

- GitHub Pages で公開中: <https://umepyon.github.io/life-roulette/>
- 新しいゲームごとにシード付きヘックスコースを生成し、PCではヘックス盤面、スマホでは選択済みの経路を後方視点の四角いマス目で表示する。
- スマホではタイトル説明を隠し、「みんなの人生」を折りたたみ式にした。幅 390px × 高さ 844px で、盤面・閉じた折りたたみ欄・サイコロボタンが初期画面に収まることを確認済み。
- 固定盤面の定義は `app.js` にフォールバック用として残しているが、通常の新規ゲームでは使わない。
- Supabase Realtime の部屋コードで、別々の端末から2〜4人で参加できる。ホストが設定した人物カードを参加順に割り当て、盤面・イベント・操作権限を同期する。

## オンライン対戦の今後の調整

- [x] Supabase Realtime Broadcast を使い、部屋コードでホスト・参加者を接続する。
- [x] 参加者ごとに操作可能なプレイヤーを限定し、サイコロ・分岐・ご祝儀・ゴール後の運命サイコロを同期する。
- [x] 参加中の新しいゲーム／終了操作を参加者側で止め、部屋の状態を保つ。
- [ ] 実機の別ネットワーク（iPhone Safari / Android Chromeを含む）で2〜4人の通しプレイを確認する。
- [ ] 部屋作成前に各参加者が人物カードと名前を選べる待機室を追加する。
- [ ] ホスト再読み込み時の再参加・ゲーム状態の保存を検討する。

## 次回優先: コース規模・イベント編集・分岐数の拡張

### 目標

ゲームを始める人が、遊ぶ時間に合わせてコースの大きさを選び、マスの出来事と金額をコード編集なしで調整できるようにする。分岐は2択に固定せず、盤面と操作画面が3択以上にも対応できる構造へ変える。

### 1. コースの大・中・小

- [x] 新しいゲームの設定画面に「小・中・大」のコース規模を追加した。
- [x] 小は現在の27マスを維持し、中は60マス、大は100マスにした。いずれもSTARTからGOALまでのメインルートのマス数で、分岐部分は別に増える。
- [x] 規模ごとに、START / GOAL / ゴール直前のSTART帰還 / 結婚 / 子ども誕生 / 3マス戻る / 1回休み / 先頭と入れ替えを配置する。
- [x] 各規模で200シードずつ、ルート長・2本の分岐・合流・STARTからGOALへの到達・不要な隣接がないことを自動検証した。実プレイ時間と収支バランスは今後調整する。
- [x] オンライン部屋では、ホストが選んだコース規模とシードを含む盤面データを全参加者へ同期する。

### 2. CSV・Excelを起点にしたイベント編集

- [x] 管理者向けCSVの読み込みを実装した。ゲーム画面からの編集は行わず、Gitプロジェクト内のCSVを公式データとして扱う。
- [x] CSVの編集項目を `id`、`type`、`label`、`sub`、`description`、`amount`、`icon`、`eventScene`、`weight`、`enabled` として定義した。
- [x] 「借金」「給料」「ビル購入」「家賃収入」などを、金額を含めてCSV上で追加・変更・無効化できるようにした。初期CSVは200件。
- [x] 読み込み時に、必須列、同じID、金額の書式、イベント種別、イベント画像名、出現頻度を検証する。失敗時は標準イベントへ戻す。
- [x] 現在のイベント候補を `data/events.csv` として用意した。読み込み失敗時も標準イベントで遊べる。
- [ ] ExcelはCSV入出力で対応する。将来 `.xlsx` を直接読み込む場合は、ブラウザ用ライブラリの追加、GitHub Pagesでの配信量、スマホでの操作性を確認してから採用する。

### 3. 分岐を3択以上へ

- [ ] コース生成器を、各分岐で2〜4本の選択肢を作れるデータ構造にする。選択肢ごとに名称、説明、行き先、合流先を持たせる。
- [ ] 小・中・大それぞれで、分岐数と3択以上の分岐数を制約として定義する。大コースほど選択の幅を増やす。
- [ ] PC盤面で接続線と赤い分岐ヘックスが重なって読めなくならない配置規則を追加する。
- [ ] スマホでは3択以上でも押しやすい選択画面にし、44px以上のタップ領域とスクロール可能な選択肢一覧を維持する。
- [ ] 2択・3択・4択のすべてで、選択後にGOALへ到達できることと、オンライン参加者本人だけが選べることを確認する。

### 完了条件

- 小・中・大のいずれでも、複数シードを生成してSTARTからGOALまで進める。
- CSVをExcelで編集して読み込んだイベント・金額が、盤面着地時とオンライン参加者全員の画面で一致する。
- 3択以上の分岐をPC・スマホ・オンライン部屋で選び、全ルートがゴール可能である。

## 優先1: iPhone・iPad・Androidへの完全対応

### 目標

タッチ操作だけで、縦向き・横向きのどちらでも遊びにくさや表示崩れがないこと。PC版の盤面、スマホ版の後方視点盤面、人物選択、イベント、分岐、ゴール結果のすべてを対象にする。

### 進捗（2026-07-31）

- [x] 代表ビューポートで確認: Android相当 360×800、iPhone相当 375×812 / 390×844 / 430×932、横向き 844×390、iPad相当 768×1024 / 1,024×768。横スクロールなしを確認した。
- [x] 横向きスマホ（幅900px以下・高さ500px以下）も、PC盤面ではなく後方視点のスマホ盤面とメニューに切り替えるようにした。
- [x] `viewport-fit=cover`、`safe-area-inset-*`、`dvh` を利用し、ノッチ・ホームインジケータ・Safariのアドレスバー伸縮に備えた。
- [x] ハンバーガー、メニュー、閉じるボタン、人数選択、サイコロを44px以上の主要タップ領域にした。`prefers-reduced-motion` ではアニメーションを最小化して状態変化は維持する。
- [x] 縦向きスマホ、横向きスマホ、iPad相当で、人物選択・後方視点盤面・サイコロ・ボード全体表示の画面内配置を確認した。
- [ ] 実機の iPhone Safari / iPad Safari / Android Chrome で、分岐・ご祝儀・ゴールまで通し操作を確認する。

### 対応項目

- iPhone Safari: 375px、390px、430px 幅で確認する。ノッチとホームインジケータの安全領域、Safari のアドレスバー伸縮、横向きも確認する。
- iPad Safari: 768px、820px、1,024px 幅で確認する。スマホ用後方視点にするか、PCボードを縮小表示するかの切替幅を実機で決める。
- Android Chrome: 360px、412px 幅で確認する。画面密度差、戻る操作、タップ領域、フォント表示を確認する。
- 横スクロールを発生させない。ボタン・分岐選択肢・人物カードは最低 44px 程度のタップ領域を確保する。
- スマホでは初期表示から後方視点盤面を見せ、プレイヤー一覧は閉じた状態から開閉できることを維持する。
- 動きを減らす設定（`prefers-reduced-motion`）でも、進行状況と操作可否が分かることを確認する。

### 完了条件

- 上記の代表幅と、可能なら実機の Safari / Chrome で、最初からゴールまで操作できる。
- 人物選択、サイコロ、分岐、ご祝儀、ゴール結果のいずれにも操作不能な重なりや画面外配置がない。
- 各画面でスクリーンショットを残して確認できる。

## 優先2: ヘックス盤面によるランダムコース生成

### 進捗（2026-07-31）

- [x] `hex-course.js` に、ヘックス座標・隣接判定・シード付き乱数・メインルート・2本の分岐と合流・妥当性検証を追加した。小27・中60・大100マスを選べる。
- [x] 同じシードで同一コースになること、および小・中・大それぞれ200種類のシードで生成できることを `node tests/hex-course.test.js` で確認した。
- [x] 生成結果をゲーム用の `next` / `routeOptions` グラフへ変換し、移動・分岐・イベント・PCヘックス盤面・スマホの経路表示へ接続した。
- [x] 新規ゲーム時にコースシードを表示し、PCとスマホ幅390pxで生成コースの開始・サイコロ・イベントを確認した。
- [ ] シードのコピー・入力・再開を追加し、画面上から同じコースを再プレイできるようにする。

### 方針

六角形（ヘックス）を土地の単位にし、新しいゲーム開始時に START から GOAL までの道を生成する。完全な無制限ランダムではなく、再現可能なシードと制約を持つ生成にする。

### 実装課題

1. ヘックス座標を定義する
   - 軸座標（`q`, `r`）などで全ヘックスを表す。
   - 各ヘックスの隣接6方向、画面座標、占有状態を管理する。
2. コースの骨組みを生成する
   - START と GOAL を離して配置する。
   - 指定した最小・最大マス数のメインルートを作る。
   - 2〜3か所の分岐と合流を入れ、行き止まりやゴール不能な道を作らない。
   - 同じマスに道を重ねない。
3. ゲーム用データへ変換する
   - 現在の `next` / `routeOptions` と互換性のあるノードグラフにする。
   - PC版はヘックス盤面と接続線を描画し、スマホ版は選ばれた経路を順番に後方視点のマス目として表示する。
4. シードを扱う
   - 新しいゲームごとにシードを発行する。
   - シードを画面に表示またはコピーできるようにし、同じコースを再現できるようにする（表示は実装済み、コピー・入力は未実装）。
5. 生成の検証を行う
   - 何百回生成しても START から GOAL へ到達できる。
   - ルート長、分岐数、合流数、各イベント種別がルール内に収まる。

### 完了条件

- [x] 新しいゲームを始めるたびに、道の形と分岐位置が変わる。
- [x] 同じシードでは、道とイベント配置が同じになる（生成器テストで確認）。
- [ ] 画面から同じシードを指定して再プレイできる。
- 生成失敗時には再試行し、遊べない盤面を表示しない。

## 優先3: 100種類程度のイベント内容をランダム配置

### 方針

道の形を生成した後、通常マスにイベント候補をランダムに割り当てる。START、GOAL、結婚、子ども誕生、分岐、特殊移動マス、ゴール直前のSTART帰還など、ゲーム進行に必須のマスは固定または配置数を保証する。

### イベントプールの例

- 小さな収入・出費
- 給料日・副収入・投資・修理費
- 大きな買い物・マイホーム・マイカー
- 宝くじなどの大当たり、予定外の損失
- 仕事、趣味、旅行、友人、家族
- 結婚・子ども誕生に関わる特別イベント

### 制約

- 同一ゲーム内の重複を基本的に避ける。
- ルートごとの収支期待値が極端に偏らないよう、プラス・マイナス・大イベントの数を配分する。
- 専用イラストが必要なイベントは、既存画像を不自然に使い回さない。足りない場面はオリジナル画像を追加する。
- マイナス所持金は許可する現在のルールを維持する。

## 優先4: テストと調整

- 2〜4人で、分岐・結婚・出産・ご祝儀・特殊移動マス・ゴール後の運命サイコロ・マイナス所持金ゴールを通して確認する。
- 乱数シードを記録し、不自然なコースや収支が出た場合に再現できるようにする。
- PC、iPhone、iPad、Android の各レイアウトで、生成した複数コースを確認する。
- 変更前に対象ファイルをコミットし、変更後は構文確認・画面確認・GitHub Pages の公開確認を行う。

## 推奨する実装順

1. iPhone・iPad・Android の代表幅／実機で、生成コースを通しで確認する。
2. イベントプールを約100種類へ増やし、収支バランスと重複回避を追加する。
3. シードのコピー・入力・再プレイを追加する。
4. イベント別の専用イラストを増やす。
